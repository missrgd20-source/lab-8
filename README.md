# lab-8
In this assignment, I loaded the dataset, explored the data, scaled the features, and split the data into training and testing sets.

Then, I trained the KNN model and checked its performance using a confusion matrix and classification report.

After that, I tested different K values to find the best one and improved the model results.
